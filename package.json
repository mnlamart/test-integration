{
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.89.0"
  },
  "scripts": {
    "css": "sass --watch styles.scss:styles.css",
    "php": "php -S localhost:8000",
    "setup": "npm install && composer install",
    "start": "npm-run-all --parallel css php"
  }
}